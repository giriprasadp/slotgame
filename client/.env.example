# Template — copy to .env and fill in values

# Local dev (default — Vite dev server proxies /api/* to localhost:3000)
VITE_API_URL=/api/v1

# Production (Railway / Vercel / Netlify — set as an env var in the platform dashboard)
# VITE_API_URL=https://your-backend.railway.app/api/v1
